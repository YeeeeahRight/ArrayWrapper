package com.epam.array.view;

import com.epam.array.util.WrapperArray;

public interface ResultPrinter {
    void printMaxElement(int element);
    void printMinElement(int element);
    void printArray(WrapperArray wrapperArray);
}
